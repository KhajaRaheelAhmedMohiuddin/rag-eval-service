# Core runtime — runs the whole service, eval harness, and tests with NO API key
# and NO heavy ML dependencies.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# Testing
pytest>=8.0
httpx>=0.27          # required by fastapi.testclient

# Optional: real OpenAI generation (only used if OPENAI_API_KEY is set)
# openai>=1.30
