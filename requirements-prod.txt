# Optional "production" RAG stack — enables the LangChain + Chroma backend with
# real embeddings (see app/backends/langchain_chroma.py). Install on top of
# requirements.txt:  pip install -r requirements-prod.txt
langchain-core>=0.3
langchain-text-splitters>=0.3
langchain-chroma>=0.1
langchain-huggingface>=0.1
langchain-openai>=0.2
sentence-transformers>=3.0
chromadb>=0.5
